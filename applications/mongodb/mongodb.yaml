kind: application
inputs:
infrastructure:
  connectivity:
    external:
      - port_info:
          port: 27017

  compute:
    spec:        
      kubernetes:
        cpu: 250m
configuration:
  start:
    script: mongodb-command.sh
  healthcheck:
    timeout: 1000
    #wait_for_ports: ALL
    script: mongodb-hc.sh
source:
  image:    
    username: adminuser
    docker_image:
       - name: mongo
         tag: latest
         username: root
  os_type: linux
spec_version: 1